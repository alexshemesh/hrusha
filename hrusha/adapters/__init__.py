"""Protocol-specific knowledge: contract addresses and, from Phase 3 on,
contract reads (positions, claimables). Public on-chain addresses only —
this package is the one place .gitleaks.toml allows raw 0x addresses."""
